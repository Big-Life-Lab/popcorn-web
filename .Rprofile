source("renv/activate.R")

# Check R version
if (getRversion() < "4.2.0") {
  warning(
    "This project requires R >= 4.2.0. ",
    "You are using R ", getRversion(), ". ",
    "Please upgrade R to ensure compatibility."
  )
}
